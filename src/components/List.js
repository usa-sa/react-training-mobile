import React, { Component } from "react";
import { View } from "react-native";

class List extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <View />;
  }
}

export default List;
